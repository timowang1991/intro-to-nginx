events {
    
}

http {
    server {
        listen 80;
        location / {
            proxy_pass https://tw.yahoo.com;
        }

        location /news {
            rewrite /news(.*) /$1 break;
            proxy_pass https://tw.news.yahoo.com;
        }
    }
}